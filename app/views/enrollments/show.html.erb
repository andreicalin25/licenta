<p style="color: green"><%= notice %></p>

<%= render @enrollment %>

<div>
  <%= link_to "Back to enrollments", enrollments_path %>

  <%= button_to "Destroy this enrollment", @enrollment, method: :delete, :onclick => "return confirm('Are you sure you want to delete this enrollment? (By doing so you will also delete all questions related to this enrollment)')" %>
</div>
